services:
  db-api:
    image: postgres:16-alpine
    ports:
      - "6543:5432"
    volumes:
      - ./database/api.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app
